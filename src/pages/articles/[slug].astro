---
/**
 * articles/[slug].astro — Dynamic article pages
 * Generates one static page per article in the content collection.
 */
import ArticleLayout from '@layouts/ArticleLayout.astro';
import { getCollection } from 'astro:content';
import { isPublished } from '@utils/publishing';

export async function getStaticPaths() {
  const articles = await getCollection('articles', isPublished);
  return articles.map((entry) => ({
    params: { slug: entry.slug },
    props:  { entry },
  }));
}

const { entry } = Astro.props;
---

<ArticleLayout entry={entry} />
